import 'package:flutter/material.dart';

class containerDay5 extends StatelessWidget {
  const imageText_box({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}